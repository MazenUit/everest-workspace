## Domain: - pure rules- 
- The business rules of the locker — sizes, “does it fit?”
- allocation
- pickup codes 


## Starting here: 
To put locker sizes and the fit rule in the domain layer so they’re testable without the API. The file started as core types plus one pure function. 
