## Compare — level 1 vs level 2

**Rules:** [`rules.ts`](./rules.ts) → `LEVEL_COMPARE_RULES`

**Metrics:** [`metrics.ts`](./metrics.ts) → `buildCompareMetrics()`

**Insight:** [`insight.ts`](./insight.ts) → `buildInsight()`

**Service:** `services/compare.ts` → `runCompare()`

**CLI:** `npm run cli:compare`
